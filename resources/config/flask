# Set Flask environment variables containing the path to the application
# and to run Flask in development mode
# This file must be used with "source dev/resources/flask" *from bash*

export FLASK_APP=robflask.api
export FLASK_ENV=development
